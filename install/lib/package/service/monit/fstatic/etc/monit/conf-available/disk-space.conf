#--- Disk usage
check filesystem fs_root with path /
    if space usage > 80% then alert
    if space usage > 85% for 5 times within 15 cycles then alert

#check filesystem fs_data2 with path /mnt/hdd/data1
#    if space usage > 80% then alert
