check program CPU-Temp with path "/etc/monit/scripts/cpu_temp.sh"
    every 10 cycles
    if status > 55 then alert

    #if status > 55 for 5 cycles then alert
    #if status > 55 4 times within 5 cycles then alert

    group health
